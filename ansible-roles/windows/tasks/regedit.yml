---
- name: Add or update registry path MyCompany, with entry 'hello', and containing 'world'
  win_regedit:
    path: "{{ registry_path }}"
    name: "{{ registry_name }}"
    data: "{{ registry_data }}"
